from setuptools import setup, find_packages

setup(
    name='multi_sum_calc',
    version='0.2',
    description='a simple python library for calculating the sum and the product of two numeric values.',
    author='Daniel Huber',
    packages=find_packages(),
)