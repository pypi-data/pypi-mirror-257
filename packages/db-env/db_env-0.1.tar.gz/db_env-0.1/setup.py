from setuptools import setup, find_packages

setup(
    name=           'db_env',
    version=        '0.1',
    description=    'library that creates a relational DB with the usage of SQLite. More information follows.',
    author=         'Daniel Huber',
    packages=find_packages(),
)
