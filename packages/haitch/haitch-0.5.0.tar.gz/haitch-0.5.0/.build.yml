image: alpine/edge
packages:
  - pipx
sources:
  - https://git.sr.ht/~loges/haitch
tasks:
  - lint: |
      cd haitch
      pipx run hatch run types:check
  - test: |
      cd haitch
      pipx run hatch run cov
