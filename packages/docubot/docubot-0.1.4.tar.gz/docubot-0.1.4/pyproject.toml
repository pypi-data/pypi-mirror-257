[tool.poetry]
name = "docubot"
version = "0.1.4"
description = "Code generating bot"
authors = ["Eyob Gemechu"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"

[tool.poetry.group.dev.dependencies]
black = "^24.2.0"

[tool.poetry.scripts]
docubot = 'docubot:docubot'

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
